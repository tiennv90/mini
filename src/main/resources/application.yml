spring:
  application:
    name: mini
  datasource:
    url: jdbc:postgresql://localhost:5433/mini
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  sql:
   init:
    mode: always
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
